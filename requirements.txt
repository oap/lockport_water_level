# Flask Water Level Dashboard Requirements
# Core web framework
Flask==3.1.2

# HTTP requests for fetching data from water office API
requests==2.32.5

# Flask dependencies (automatically installed with Flask but listed for clarity)
blinker==1.9.0
click==8.2.1
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.2
Werkzeug==3.1.3

# HTTP/SSL dependencies
certifi==2025.8.3
charset-normalizer==3.4.3
idna==3.10
urllib3==2.5.0

# Note: The following standard library modules are also used but don't require installation:
# - sqlite3 (built into Python)
# - csv (built into Python) 
# - datetime (built into Python)
# - threading (built into Python)
# - logging (built into Python)
# - os (built into Python)
# - io (built into Python)
